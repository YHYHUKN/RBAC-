# ===================== Node / Frontend (Vite) =====================
node_modules/
dist/
dist-ssr/
*.local
.env.local
.env.*.local
# 注：.env.production 为本项目演示配置（仅 VITE_MOCK=true），予以保留

# ===================== Maven / Backend (Spring Boot) =====================
target/
*.class
*.jar
*.war
!**/src/**/*.class

# ===================== IDE =====================
.idea/
.vscode/
*.iml
.project
.classpath
.settings/

# ===================== OS =====================
.DS_Store
Thumbs.db
ehthumbs.db

# ===================== Logs =====================
*.log
app.log

# ===================== Build / Misc =====================
.build-verify/
*.tmp
# Vite 加载配置时生成的临时编译文件
vite.config.js.timestamp-*.mjs
*.timestamp-*.mjs
